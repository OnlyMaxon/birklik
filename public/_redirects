# STATIC ASSETS
/assets/* /assets/:splat 200
/brand/* /brand/:splat 200

# SYSTEM FILES  
/sw.js /sw.js 200
/favicon.ico /favicon.ico 200

# SPA CATCH-ALL (MUST BE LAST)
# Rewrite all non-existent routes to index.html with 200 status
# This allows React Router to handle the navigation
/* /index.html 200
