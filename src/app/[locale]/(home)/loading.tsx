import {HomePageSkeleton} from '@/components'

/**
 * Заглушка только для главной — ради этого вокруг неё и заведена группа
 * `(home)`: на адрес она не влияет, но ограничивает область действия файла.
 *
 * Раньше этот же файл лежал в корне `src/app/` и накрывал собой ВСЁ. Отсюда две
 * беды сразу.
 *
 * Первая, косметическая: при переходе на страницу региона или на объявление без
 * своей заглушки показывался скелет главной — шапка с поиском на месте будущего
 * списка.
 *
 * Вторая, важная: заглушка создаёт границу Suspense, и Next отправляет ответ
 * сразу, не дожидаясь страницы. Заголовки уходят с кодом 200 ещё до того, как
 * страница вызовет `notFound()`, и поменять статус уже нечем. Несуществующее
 * объявление, выдуманный регион и просто опечатка в адресе — всё отвечало 200,
 * то есть приглашало поисковик индексировать несуществующее.
 *
 * Теперь граница только вокруг главной, а главная `notFound()` не вызывает.
 */
export default function HomeLoading() {
  return <HomePageSkeleton />
}
