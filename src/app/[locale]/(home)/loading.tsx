import {HomePageSkeleton} from '@/components'

/**
 * Заглушка главной. Ради неё вокруг страницы и заведена группа `(home)`: на
 * адрес группа не влияет, но ограничивает область действия файла.
 *
 * До 2026-09-22 её перекрывал корневой `src/app/loading.tsx` — одна заглушка на
 * всё приложение, стоявшая вынужденно ради запаса по процессорному времени
 * тарифа Workers Free. С переходом на Workers Paid корневой файл удалён, и
 * работает уже эта: главная сохранила скелет, а остальные маршруты получили
 * честные коды ответа.
 *
 * Косметическая мелочь, которую разделение заодно починило: с общей заглушкой
 * при переходе на страницу региона показывался скелет ГЛАВНОЙ — шапка с поиском
 * на месте будущего списка. Теперь у региона свой скелет.
 */
export default function HomeLoading() {
  return <HomePageSkeleton />
}
