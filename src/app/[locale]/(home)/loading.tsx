import {HomePageSkeleton} from '@/components'

/**
 * Заглушка главной. Ради неё вокруг страницы и заведена группа `(home)`: на
 * адрес группа не влияет, но ограничивает область действия файла.
 *
 * Сейчас она дублирует корневой `src/app/loading.tsx` и потому ни на что не
 * влияет. Оставлена намеренно: корневой файл стоит вынужденно, ради потоковой
 * отдачи и запаса по процессорному времени, и уйдёт при переходе на платный
 * тариф. Вот тогда эта заглушка и начнёт работать — главная сохранит скелет, а
 * остальные маршруты получат честные коды ответа.
 *
 * Косметическая мелочь, которую она заодно чинит: пока заглушка одна на всё
 * приложение, при переходе на страницу региона показывается скелет ГЛАВНОЙ —
 * шапка с поиском на месте будущего списка.
 */
export default function HomeLoading() {
  return <HomePageSkeleton />
}
