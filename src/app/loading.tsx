import {HomePageSkeleton} from '@/components'

/**
 * Заглушка ожидания в корне приложения. Стоит здесь ВЫНУЖДЕННО.
 *
 * Она создаёт границу Suspense, а та включает потоковую отдачу: воркер
 * отправляет оболочку страницы, не дожидаясь, пока досчитается всё остальное.
 * На тарифе Workers Free запрос ограничен **10 мс процессорного времени**, и без
 * этой границы отрисовка целиком не укладывается — Cloudflare обрывает ответ с
 * ошибкой 1102 (`exceededCpu`).
 *
 * Замерено на боевом 2026-08-30, по 20 запросов главной:
 *
 * ```
 * с этим файлом    0 ошибок из 20
 * без него        10 ошибок из 20
 * ```
 *
 * Плата за него — коды ответа. Заголовки уходят раньше, чем страница успевает
 * вызвать `notFound()`, поэтому несуществующее объявление и выдуманный регион
 * отдают 200 вместо 404. От индексации они закрыты `noindex` в
 * `generateMetadata` — см. `property/[id]/page.tsx` и `[locale]/kiraye/[city]`.
 *
 * **Как включить настоящие 404.** Перейти на Workers Paid (там лимит 30 секунд
 * вместо 10 мс) и удалить этот файл. Всё остальное для честных кодов ответа уже
 * на месте: заглушка главной живёт отдельно в `[locale]/(home)/loading.tsx`, а
 * `property/[id]/layout.tsx` проверяет существование до границы Suspense.
 * Проверено — работает, упирается только в тариф.
 */
export default function AppLoading() {
  return <HomePageSkeleton />
}
